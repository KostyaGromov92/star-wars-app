/* eslint-disable max-len */
/* eslint-disable import/prefer-default-export */

// STAR SHIPS - SELECTORS
// =============================================================================

export const getStarShipsList = (state) => (state.starships  ? state.starships.starShipsList : []);

